# 追補指示書: BITリンクの動線修正(永続URLが存在しない前提へ)

対象: 競売機能(`api/auction-import.js` / `api/auction-list.js` / 競売タブUI / 管理者セクション)。
現在オープン中の競売関連ブランチがあればその上で、無ければ新しいブランチで実装すること。§0の絶対規則は引き続き適用。

## 判明した事実

BITの物件詳細画面のURLは、どの物件を開いても `https://www.bit.courts.go.jp/app/propertyresult/pr001/h05` で共通であり、**物件を特定する永続URLが存在しない**(検索フォームからのPOST+セッションで結果を保持する構造のため)。

現状、登録済み物件の「BITで3点セットを見る↗」を押すと、全物件で「アクセス頂きましたURLは、現在ご利用できません。」のエラー画面になる。**リンクによる直接遷移は原理的に不可能**であるため、動線を変更する。

## 変更内容

### 1. `bit_url` を必須から任意へ

- 管理者フォーム(方式A)および貼り付け取り込み(方式C)で、**`bit_url` を任意項目に変更**する
- サーバー `api/auction-import.js` の必須項目を **`case_no` のみ**に変更する(`bit_url` を必須から外す)
- `bit_url` が入力された場合の検証(https かつ `www.bit.courts.go.jp` 配下のみ許可)は**そのまま維持**する
- フォームのラベルを「BITの物件URL(任意・BITは物件ごとの固定URLが無いため通常は空欄)」に変更し、赤字の必須案内は削除する

### 2. 結果カードのボタンを「BITで検索」に変更

「BITで3点セットを見る↗」を廃止し、代わりに **「BITで検索(事件番号をコピー)」** ボタンを設置する。挙動:

1. `navigator.clipboard.writeText(case_no)` で事件番号をクリップボードへコピーする
   - `navigator.clipboard` が使えない環境では、一時的な `<textarea>` + `document.execCommand("copy")` にフォールバックする
2. `window.open("https://www.bit.courts.go.jp/", "_blank", "noopener")` でBITを新しいタブで開く
3. カード上に3秒間のトーストまたはインライン表示で「事件番号をコピーしました。BITの検索画面で貼り付けてください」と表示する

補足表示として、カードに事件番号を常に表示し、その横に小さなコピーアイコン(クリックで事件番号のみコピー)を置く。

### 3. `bit_url` が登録されている場合の扱い

- 過去に登録された `bit_url` を持つレコードがあるため、値が入っている場合のみ「登録リンクを開く」という**副次的なリンク**を小さく併記する(将来BITの仕様が変わった場合に備える)
- ただし主動線は上記2の「BITで検索」とする

### 4. 文言の整理

- 管理者セクション冒頭の注意文に一行追加: 「BITは物件ごとの固定URLを持たないため、URL欄は通常空欄で構いません。」
- 競売タブの免責文はそのまま維持する

## 受け入れテスト(PR説明に記載)

1. `npm run build` 成功
2. `bit_url` を空欄のまま、方式Aのフォームから物件を登録できる
3. `bit_url` 列を空にした貼り付けデータで、方式Cから一括登録できる
4. `bit_url` に BIT以外のURLを入れた場合は従来どおり拒否される
5. 結果カードの「BITで検索(事件番号をコピー)」を押すと、事件番号がクリップボードにコピーされ、BITのトップページが新しいタブで開き、コピー済みの案内が表示される
6. 事件番号を持たないレコードでは、当該ボタンが無効化される(または表示されない)
7. `bit_url` が登録済みのレコードでは、副次リンクが併記される
8. 一般ユーザー(Pro)の検索・閲覧・フォロー・カレンダー連携・入札上限計算に回帰がない
