# 指示書: 初回体験の整備とプラン境界の見直し (PR#16)

対象: `realsim-app`。ブランチ `agent/pr16-onboarding` を新規に切って作業する。

## 0. 背景

新規ユーザーがサインアップすると、6つのタブと空の画面だけが並び、何をすればよいか分からない。加えて、収支の詳細モード（30以上のパラメータ）がPro限定になっているが、**この機能の限界費用はゼロ**（計算はすべて `src/engine.js` のクライアント側処理）であり、隠しても運用コストは1円も下がらない。一方でLPは「30以上の前提を織り込む」ことを主要な訴求にしており、無料ユーザーはそれを検証できないまま課金を求められている。

本PRで次の2つを行う。

1. **詳細モードをFreeに開放する**（分析・運用管理・競売・PDFレポートはProのまま）
2. **サンプルデータを用意し、初回から動いている状態のアプリに着地させる**

AI市場調査はAPI従量課金があるためProのままとし、代わりに**API を呼ばないサンプル結果**を用意して、Freeユーザーでも機能の中身を見られるようにする。

---

## 1. `src/plan.js` — 権限をフラグ化する

`PLANS` に機能フラグを追加し、判定用のヘルパーを export する。**`SALT` と `verifyLicense` には触れないこと**（発行済みライセンスキーが無効になる）。

```js
export const PLANS = {
  free: {
    id: "free", label: "Free", maxProperties: 3, aiPerMonth: 0,
    detailMode: true,   // ← 開放
    analysis: false, ops: false, auction: false, report: false,
  },
  pro: {
    id: "pro", label: "Pro", maxProperties: Infinity, aiPerMonth: 10,
    detailMode: true,
    analysis: true, ops: true, auction: true, report: true,
  },
};

/** プランがその機能を使えるか */
export function can(plan, feature) {
  return !!(PLANS[plan] || PLANS.free)[feature];
}
```

**本PRで `can()` に置き換えるのは詳細モードの判定だけ。** 分析・運用管理・競売・レポートの出し分けは、既存の `isPro` のままにしておくこと。全面的な移行はしない（差分が膨らみレビュー不能になるため）。

---

## 2. 詳細モードのゲートを外す（`src/App.jsx`）

### 2.1 `switchMode`（L2375付近）

```js
const switchMode = (k) => {
  if (k === "pro" && !isPro) { setUpgradeOpen(true); return; }   // ← この行を削除
  setMode(k); saveKey("ui-mode", k);
};
```

`if` の行を削除する。アップグレードモーダルはもう開かない。

### 2.2 プラン降格時の副作用（L2348付近）

```js
if (tab === "ana" || tab === "ops" || tab === "auc") setTab("sim");
if (mode === "pro") setMode("easy");   // ← この行を削除
```

タブの退出はそのまま残し、**モードを easy に戻す行だけ削除**する。

### 2.3 命名についての注意

モードの内部値は `"easy"` / `"pro"` で、この `"pro"` は**Proプランとは無関係**（「詳細モード」の意）。`localStorage` の `ui-mode` に保存済みの値があるため、**値の名前を変更してはいけない**。誤解を避けるため、`switchMode` の直前に「この "pro" は詳細モードの意味であり、プランとは無関係」というコメントを1行入れること。

### 2.4 モード切替のUI（L2717付近）

説明文を差し替える。

```
easy: 「3項目だけで診断できます。残りの前提は保守的な値で自動設定されています」
pro : 「30以上の前提をすべて自分で設定できます。Freeプランでも使えます」
```

---

## 3. `src/demoData.js` を新規作成する

**静的なJavaScriptオブジェクトとして持つ。APIもDBも使わない。**

```js
// サンプル物件のパラメータ。形は既存のシミュレーション用パラメータ(p)と完全に一致させる
export const DEMO_PROPERTY_PARAMS = { /* … */ };

// サンプルのAI市場調査結果。形は api/research.js が返すデータ(d)と完全に一致させる
export const DEMO_RESEARCH = { /* … */ };
```

### 形を勝手に決めないこと

- `DEMO_PROPERTY_PARAMS` のフィールド名と型は、**`src/engine.js` の既定パラメータと `src/App.jsx` の `p` の初期値から導出する**こと
- `DEMO_RESEARCH` のフィールド名は、**`src/App.jsx` の `applyData(d)` が読んでいるキーすべて**と、`AiValues` コンポーネントが表示しているキーすべてを網羅すること。1つでも欠けると `undefined` になり、反映時に既定値へ落ちる
- 存在しないキーを追加しないこと

### サンプル物件の中身

「業者から提案されがちな新築ワンルーム」を題材にする。診断が**黄信号**になる値にすること（青信号だと危険の見せ方が伝わらず、赤信号だと絶望的に見える）。

- 想定: 東京23区・中古ワンルーム・築浅
- 物件価格 2,480万円 / 月額家賃 92,000円 / 自己資金 100万円 前後
- 実装後、実際に診断が黄信号になることを計算で確認し、ならなければ値を微調整してよい

### サンプルAI結果の中身

エリアは「東京都文京区」、物件タイプは「中古ワンルーム」とする。値は実在の統計を装わず、**もっともらしい保守的な参考値**にとどめること。

---

## 4. サンプル物件を初回に投入する（`src/App.jsx`）

### 投入条件

初回のデータ読み込み後、**次の両方を満たすときだけ**投入する。

- `KEY_PROPS` の物件が **0件**
- 新しいキー `demo-seeded` が未設定

投入したら `saveKey("demo-seeded", true)` を書き込む。**一度削除したサンプルが再び湧かないようにするため、このフラグは必須。**

### レコードの形

既存の物件レコード（`{ id, name, savedAt, params }`）に **`isDemo: true`** を足す。`name` は `"サンプル: 文京区 1R"` のような、サンプルと分かる名前にする。

### サンプル物件の扱い（重要）

- **`owned` のチェックボックスを操作できないようにする**（`disabled`）。保有ポートフォリオのサマリーと純資産トラッカーは `owned` で絞り込んでいるため、これだけで数字が汚れなくなる
- 行に「**サンプル**」バッジを表示する（`T.goldSoft` 背景 / `T.aiInk` 文字）
- **`saveCurrentProperty` の件数チェックから除外する。** `properties.length >= PLANS[plan].maxProperties` を、`isDemo` を除いた件数で判定するよう変更する
- 物件比較タブでは通常どおり選べてよい（比較の使い方が伝わる）

### 削除と再表示

- サンプル物件の行に「**サンプルを削除**」ボタンを置く。押すと通常の `deleteProperty` で消える
- `AccountModal`（設定）に「**サンプルデータを再表示**」を追加する。押すと `demo-seeded` を消して再投入する

---

## 5. サンプルAI結果を見せる（`src/App.jsx`）

### ボタンを追加する

AI市場データのパネル（L2783付近）の既存ボタン「市場データを調査する」の**隣に**、もう1つ置く。

```
ラベル: 「サンプル結果を見る（API消費なし）」
配色:  btnSt の outline 系（既存の T.navy ボタンより弱く見せる）
```

押したときの動作は次のとおり。

- `setAiState({ status: "done", data: DEMO_RESEARCH, error: null, isDemo: true })`
- **APIを呼ばない**
- **`aiQuota().inc()` を呼ばない / `refreshProfile()` を呼ばない**（回数を消費しない）
- **`KEY_RESEARCH` に保存しない**（保存済みリサーチライブラリを汚さない）
- `!isPro` でも `authEnabled && !user` でも押せる。ログインもプランも問わない

### 「これはサンプルです」を常時出す

`aiState.isDemo === true` のとき、結果パネルの**上部に常時**次を表示する。折りたたまない。閉じられるようにしない。

```
これは操作を体験するためのサンプルです。実在のエリア調査結果ではありません。
実際の調査は「市場データを調査する」から実行できます（Proプラン・月10回）。
```

配色は `T.warnBg` 背景 / `T.warnInk` 文字 / `T.warnLine` 枠。

### 反映は許可する

「推定値をシミュレーションへ反映」は `isDemo` のときも押せてよい。触って理解してもらうのが目的のため。ただし反映後も上の注記は消さないこと。

### 既存の `runFetch` は変更しない

本物の調査の経路（`!isPro` で `setUpgradeOpen`、回数チェック、ライブラリ保存）には**一切手を入れないこと**。

---

## 6. 各タブに1行の説明を置く

タブの中身の一番上に、閉じられる1行の説明ストリップを表示する。**ガイドツアーは作らない。**

- 閉じた状態は `localStorage` に `ui-tabhint-<tabキー>` として保存する（端末ローカルでよい。`saveKey` は使わない）
- 配色は `T.aiBg` 背景 / `T.aiLine` 枠 / `T.ink` 文字、`fontSize: 12.5`
- 右端に閉じるボタン（`icons.jsx` の `Icon name="x"`）

| タブ | 文言 |
|---|---|
| home | 保有している物件と、気になっている候補をここで一覧します。まずはサンプル物件を開いてみてください。 |
| sim | 1つの物件について、35年分の収支を月単位で計算します。かんたんモードは3項目、詳細モードでは30以上の前提を自分で設定できます。 |
| cmp | 保存した物件を同じ前提で横に並べ、IRR・DSCR・総合損益で比べます。 |
| ana | どの前提が結果を最も動かすか、悪条件が重なっても耐えるか、何年目に売るのが得かを調べます。 |
| ops | 買った後の管理です。計画と実績の差、設備の交換時期、確定申告用の集計をここで扱います。 |
| auc | 管理者が登録した競売物件から、収支を逆算して入札の上限額を出します。 |

---

## やらないこと

- `src/engine.js` の変更（計算結果が1円でも変わる変更は却下）
- `src/plan.js` の `SALT` と `verifyLicense` の変更
- **分析・運用管理・競売・PDFレポートのゲートを開けること**（Proのまま）
- `runFetch`（本物のAI調査）の経路の変更
- ガイドツアー・スポットライト・ステップ順送りの実装
- `src/App.jsx` の分割リファクタリング
- `src/logo.jsx` `src/icons.jsx` `src/assets/` `public/` の変更
- 新規npmパッケージの追加
- 1ヶ月無料トライアルの実装（プラン状態が `localStorage` 管理のため、現状では成立しない。別途対応する）

---

## 完了条件（Codexが機械的に確認すること）

コマンドを実行し、**結果をPR説明に貼ること**。

- [ ] `npm run build` が成功する
- [ ] `git diff src/engine.js` が **空**
- [ ] `git diff src/logo.jsx src/icons.jsx` が **空**
- [ ] `git diff src/plan.js | grep -E 'SALT|verifyLicense'` が **空**
- [ ] `git diff --stat` の変更ファイルが `src/plan.js` / `src/App.jsx` / `src/demoData.js` の3つ（必要なら `src/ui.jsx`）に収まっている
- [ ] `package.json` に差分が無い
- [ ] `grep -n 'k === "pro" && !isPro' src/App.jsx` が **0件**（詳細モードのゲートが外れている）
- [ ] `grep -n 'setMode("easy")' src/App.jsx` が **0件**
- [ ] `grep -n 'demo-seeded' src/App.jsx` が **2件以上**（投入時の書き込みと、再表示時の削除）
- [ ] `DEMO_RESEARCH` のキー集合が、`applyData(d)` が読むキーと `AiValues` が表示するキーを**すべて含む**こと。差分があれば、その一覧をPR説明に書く
- [ ] サンプル物件の投入処理が `isDemo: true` を付けていること
- [ ] `saveCurrentProperty` の件数判定が `isDemo` を除外していること

## 報告すること

**スクリーンショットは不要。ブラウザでの画面確認も不要。**

1. 上の各コマンドの実行結果
2. 変更箇所ごとに、どのファイルの何行目をどう変えたかの対応表
3. `DEMO_PROPERTY_PARAMS` と `DEMO_RESEARCH` の各フィールドを、**どのコードから導出したか**（`engine.js` の何行目、`applyData` の何行目、など）
4. サンプル物件の診断が黄信号になることを、どう確認したか（計算した値も書く）
5. 指示書とリポジトリの実態が食い違っていた箇所

## 人間がプレビューで確認すること（Codexは判定しない）

- Freeプランで詳細モードに切り替えられ、30以上のパラメータが編集できる
- 分析・運用管理・競売タブは、Freeでは従来どおりロックされたまま
- 新規アカウントでログインすると、サンプル物件が1件入っていて診断が黄信号になる
- サンプル物件の「保有中」チェックが押せず、純資産トラッカーの数字が0のまま
- 「サンプル結果を見る」を押してもAI調査の残り回数が減らない
- サンプル結果に「これはサンプルです」の注記が常時出ている
- 保存済みリサーチライブラリにサンプルが混ざっていない
- サンプルを削除して再読み込みしても復活しない。設定から再表示できる
- 各タブの説明ストリップが閉じられ、再読み込みしても閉じたままになる
