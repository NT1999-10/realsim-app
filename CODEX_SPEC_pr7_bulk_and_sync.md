# 指示書: 競売データ入力の一括化(PR#7)+ 日次自動同期(PR#8・任意)

`CODEX_SPEC_phase1-3.md` の §0 絶対規則はすべて引き続き適用。**PR#7 と PR#8 は別PRに分けること**。PR#7を先に完成させ、報告して停止すること。

## 背景

管理者が競売物件を1件ずつフォーム入力する運用は現実的でない。BITの検索結果を**まとめて取り込む**手段を用意する。設計思想(公表情報の転記・3点セットPDFは保存せずリンクのみ)は不変。

---

# PR#7: 貼り付け一括取り込み(最優先・すぐ効く)

## 7-1. 表の貼り付け取り込み(方式C)

既存の管理者セクションに、方式A(フォーム)・方式B(CSV)に加えて **方式C「表を貼り付けて一括登録」** を追加し、これを**既定の推奨方式として最上部に配置**する(方式Aは1件修正用に残す)。

- 大きめのtextarea 1つ +「解析する」ボタン
- 案内文: 「BITの検索結果ページで表の範囲を選択して Ctrl+C し、そのまま貼り付けてください。Excelからの貼り付け(タブ区切り)、CSV、BITからダウンロードしたファイルの中身にも対応します。」

### パース要件(すべて自動判別)

1. **区切り文字の自動判別**: タブ / カンマ / 全角カンマ。1行目に最も多く現れる区切りを採用
2. **ヘッダー行の自動判別**: 1行目が既知の列名(英語キー or 日本語表記)を2つ以上含めばヘッダーとして扱う。含まなければ**ヘッダー無し**とみなし、列順は `CSV_COLUMNS` の並びと仮定
3. **日本語ヘッダーの吸収**(部分一致・スペース/括弧/全角半角を無視して照合):
   - 事件番号 → case_no ／ 裁判所・裁判所名 → court ／ 物件番号 → item_no
   - 所在地・所在・住所 → address ／ 都道府県 → pref ／ 市区町村・市町村 → city
   - 種別・種類・物件種別 → type
   - 売却基準価額・売却基準額・基準価額 → min_price
   - 買受申出保証額・保証額 → deposit
   - 入札期間開始・入札開始 → bid_start ／ 入札期間終了・入札締切・入札終了 → bid_end
   - 開札期日・開札日 → open_date
   - 築年・建築年 → built_year ／ 建物面積・専有面積・床面積 → floor_area ／ 土地面積・敷地面積 → land_area
   - URL・物件URL・リンク → bit_url
4. **入札期間が1セル("2026/08/04〜2026/08/12" 等)の場合**、`〜`・`~`・`-`・`から` で分割して bid_start / bid_end に割り当てる
5. **日付形式**: `YYYY-MM-DD` `YYYY/MM/DD` `YYYY年M月D日` `令和7年8月4日`(和暦は令和のみ対応・令和N年 = 2018+N年)をすべて受理し ISO(YYYY-MM-DD)へ正規化
6. **金額**: 全角数字・カンマ・「円」「万円」の単位表記を除去。**「万円」の文字が含まれる、または値が100,000未満の場合は万円とみなして10,000倍**し、内部は常に円で保持する(判定結果はプレビューに明示)
7. **面積**: 「㎡」「m2」「平方メートル」を除去して数値化
8. **type の正規化**: 値に「区分所有」または「マンション」を含む→マンション ／「土地」と「建物」の両方を含む→戸建て ／「一戸建」「戸建」を含む→戸建て ／「土地」のみ→土地 ／ それ以外→その他
9. **id**: 空欄なら `court + case_no + item_no` から自動生成(既存APIのid規約に合わせる)。**ユーザーにidの入力を要求しない**
10. **active**: 空欄なら true

### プレビューと確定

- 解析後、**登録せずにプレビュー表を表示**する。列: 行番号 / 所在地 / 種別 / 売却基準価額(円・万円換算も併記) / 入札期間 / 開札日 / bit_url / 判定
- 判定は OK / 警告(任意項目の欠落・単位の推測を行った等) / **エラー**(必須の case_no・bit_url が無い、bit_urlがBIT以外、日付が不正)
- エラー行は**チェックボックスが自動でオフ**になり登録対象から外れる。理由を行ごとに日本語で表示
- プレビュー表の各セルは**その場で修正可能**(input)にする。修正すると判定が再評価される
- 「選択した N 件を登録する」ボタンで既存 `/api/auction-import` へ送信

## 7-2. テンプレート配布

管理者セクションに「**CSVテンプレートをダウンロード**」ボタンを置き、ヘッダー行+記入例1行のCSV(UTF-8 BOM付き)をクライアント側で生成してダウンロードさせる。列名と単位(min_price/depositは円)、type の4値、日付はYYYY-MM-DD、をコメント無しで表現できるよう記入例で示す。

## 7-3. サーバー側の受け入れ緩和

`/api/auction-import` を以下のとおり緩和する(セキュリティは維持):

- `id` が空の場合はサーバー側でも `court+case_no+item_no` から生成する
- 必須は `case_no` と `bit_url` の2つに変更(idは自動生成できるため)
- 既存の検証(bit_urlはhttpsかつ `www.bit.courts.go.jp` 配下のみ、MAX_ROWS=1000、BATCH_SIZE=200、管理者JWT + ADMIN_EMAILS の二重確認)は**すべて維持**
- 同一idが既に存在する場合は上書き(upsert)し、レスポンスに `{inserted, updated, skipped, errors:[{row, reason}]}` を返す。クライアントは登録後にこの内訳を表示する

## 7-4. 受け入れテスト(PR説明に記載)

1. `npm run build` 成功
2. BITの検索結果ページの表を選択コピーして貼り付け → 解析され、プレビューに複数行が並ぶ
3. 「1,289万円」「12,890,000円」「12890000」のいずれの表記でも同じ円額として解釈される
4. 「令和8年8月4日〜令和8年8月12日」形式の入札期間が bid_start/bid_end に正しく分割・正規化される
5. bit_url がBIT以外の行はエラー判定になり、チェックが外れる
6. エラー行を含む貼り付けでも、正常行だけを選んで登録できる
7. 同じデータを2回登録しても重複せず `updated` としてカウントされる
8. 非管理者・未ログインからのAPI直接POSTは従来どおり401/403

---

# PR#8: 日次自動同期(PR#7完了後・別PR)

**PR#7が完成しマージされるまで着手しないこと。**

## 8-1. 実行環境

Vercelのサーバーレス関数(10秒制限)ではヘッドレスブラウザを実行できないため、**GitHub Actions のスケジュール実行**で行う。

- `.github/workflows/auction-sync.yml` を新設。`schedule: cron: "0 18 * * *"`(UTC18時 = JST翌3時)+ `workflow_dispatch`(手動実行可)
- Node + Playwright(chromium)をセットアップし、`scripts/auction-sync.mjs` を実行
- Secrets: `ADMIN_IMPORT_TOKEN`(下記)、`APP_BASE_URL`、`AUCTION_PREFS`(カンマ区切り、既定は東京都・神奈川県・埼玉県・千葉県)

## 8-2. 取得ルール(§4.1の遵守事項。緩和禁止)

- **1リクエストあたり3秒以上の間隔**を必ず空ける(sleep実装をレビューで確認できる形に)
- 1実行あたり**最大50ページ**まで。超えたら打ち切ってログに残す
- User-Agent: `RealSimBot/1.0 (+<運営者メール>)` を明示
- **3点セットPDFは取得も保存もしない**。物件ページのURLのみ保持
- 取得失敗・DOM構造変化時は例外を投げずにスキップし、ログに記録。**リトライは1物件につき1回まで**
- 取得対象は検索結果の一覧項目のみ(事件番号・所在地・種別・売却基準価額・入札期間・開札期日・物件URL)

## 8-3. 認証と投入

- スクリプトは収集結果をJSONにまとめ、`POST {APP_BASE_URL}/api/auction-import` へ送信する
- **管理者JWTの代わりに、機械用の共有シークレットで認証する**: `api/auction-import.js` に「`Authorization: Bearer <ADMIN_IMPORT_TOKEN>` が環境変数 `ADMIN_IMPORT_TOKEN` と一致する場合も許可」する分岐を追加。`ADMIN_IMPORT_TOKEN` はVercel環境変数(Sensitive)とGitHub Secretsの両方に同じ値を設定する
- **既存の管理者JWT+ADMIN_EMAILS経路は残す**(人間の操作用)
- 今回の走査で検出されなかった既存のactiveレコードは、**同期が正常完了した場合のみ** `active=false` にする(途中失敗時は既存データを消さない)

## 8-4. 受け入れテスト

1. `workflow_dispatch` で手動実行して成功し、`auction_items` に行が入る
2. 2回目の実行で重複せずupsertされる
3. `ADMIN_IMPORT_TOKEN` を誤った値にすると401
4. リクエスト間隔3秒以上・50ページ上限がコード上で保証されている
5. 途中で例外が発生した場合、既存レコードが `active=false` にされない
