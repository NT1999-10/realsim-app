# 追補指示書: PR#9 — PDF画像化の品質不足を修正(OCR不能の解消)

対象: `agent/pr9-pdf-intake` ブランチ(PR #9)。**同じブランチ上で修正**すること。§0の絶対規則は引き続き適用。
本書は `CODEX_SPEC_pr9_pdf_intake.md` の §A-2(画像化の指定)を**上書き**する。

## 症状

実物の3点セットPDF(裁判所のスキャン)をアップロードしたところ、解析は最後まで走ったが、モデルが「画像が非常に薄く、文字の判読が困難なため、ほとんどの項目を抽出できなかった」と返し、全項目がnullになった。認証・API呼び出し・JSON解析・フォーム反映は正常に動作している。**画像化の品質のみが原因**である。

## 原因の候補(すべて潰すこと)

1. レンダリング倍率の計算誤り。`scale` を1以下にキャップしていると、PDFの論理サイズ(A4≒595pt)そのままの低解像度画像になり判読不能になる
2. 目標幅1600px・JPEG品質0.65が、元が低コントラストなグレースケールのスキャンに対して不足していた
3. canvasに白背景を敷かずにJPEG化している(透明部分が黒く潰れる、または全体が沈む)
4. コントラスト補正を行っていないため、薄いスキャンがそのまま送られている

## 修正内容

### 1. 解像度の保証(最重要)

- 目標幅を **2000px** に引き上げる
- 倍率は `scale = TARGET_WIDTH / viewport.width` で算出し、**1以下にキャップしない**(必ず拡大方向にも働かせる)。上限は `scale <= 6` 程度のガードのみ
- 生成後、**実際のcanvas幅・高さを `console.log` に出力**する(1900〜2100px程度になっていることを確認できるように)

### 2. 白背景 + グレースケール + コントラスト補正

canvasへ描画する処理を次の順で行う:

1. `ctx.fillStyle = "#FFFFFF"; ctx.fillRect(0,0,w,h)` で**白で塗りつぶしてから** `page.render` する
2. 描画後、`ctx.getImageData` で画素を取得し、以下を適用する
   - グレースケール化: `v = 0.299R + 0.587G + 0.114B`
   - **レベル補正(コントラスト伸張)**: 画素のヒストグラムから下位2%点を `lo`、上位2%点を `hi` とし、`v' = clamp((v - lo) / (hi - lo) * 255, 0, 255)` で伸張する。`hi - lo < 40` の場合は補正を行わない(白紙ページ等での破綻防止)
   - 伸張後の値をR=G=Bに書き戻して `putImageData`
3. その上で `canvas.toDataURL("image/jpeg", 0.8)` とする

グレースケール化によりJPEGの色差成分が均一になり、品質を0.8に上げてもファイルサイズはむしろ小さくなる。

### 3. ページ数と容量の再設計

- 基本ページ: **1〜6ページ**(8→6に削減。解像度を優先する)
- 評価書の開始ページ指定時: そこから **6ページ**を追加(合計最大12枚)
- 総ペイロードの上限判定は従来どおり3.5MB。超過時の縮小順序を次のように変更する:
  1. 目標幅を 2000 → 1700 に下げて再生成
  2. まだ超過なら 1700 → 1500
  3. まだ超過ならエラー「ページ数を減らしてください」
  **JPEG品質は0.8から下げない**(文字の判読性は品質より解像度・コントラストに依存するため)
- サーバー側の枚数上限を16→**12**に合わせ、1枚あたりの上限は1.5MBに緩和する

### 4. 送信前プレビュー(重要な診断手段)

生成した**1枚目の画像を、方式Dのフォーム内にサムネイル(幅300px程度)として表示**し、その下に「この画質でAIに送信します。文字が読めない場合は解析に失敗します」と添える。

- 管理者が送信前に判読可能かを目視でき、失敗の切り分けが即座にできる
- サムネイルはクリックで原寸表示(新規タブでdata URLを開く等)できるとなお良い
- 解析後もサムネイルは残し、再解析時に差し替える

### 5. プロンプトの微修正

抽出指示テキストの冒頭に次を追加する:

> これは日本の裁判所が作成した不動産競売の「3点セット」をスキャンした画像です。薄い印字や罫線があっても、読み取れる範囲で正確に抽出してください。数字は桁を誤らないよう特に慎重に読み取ってください。

`notes` に「画像が薄い」等の**解析メタ情報を書かせない**よう、次も明記する:

> notes には物件自体の注意事項のみを記載し、画像品質や抽出可否についての所感は含めないこと。

## 受け入れテスト(PR説明に記載)

1. `npm run build` 成功
2. 実物の3点セット(41ページ・薄いスキャン)をページ指定なしでアップロード → サムネイルで文字が判読できる
3. `console.log` に出力されたcanvas幅が1900〜2100pxの範囲にある
4. 解析結果に事件番号・売却基準価額・買受可能価額・保証額・入札期間・開札日・所在地・固定資産税・都市計画税が入る(nullでない)
5. 評価書の開始ページを指定して再解析 → 評価額・築年・用途地域・建蔽率/容積率が入る
6. 送信ペイロードが3.5MB以下(Networkタブで確認)
7. `notes` に画像品質に関する記述が含まれない
