# 追補指示書: PR#9 — 画質補正で文字が消える不具合の修正

対象: `agent/pr9-pdf-intake` ブランチ(PR #9)。**同じブランチ上で修正**すること。§0の絶対規則は引き続き適用。
本書は `CODEX_SPEC_pr9b_image_quality_fix.md` の §2(コントラスト補正)を**上書き**する。

## 症状

送信前プレビューを確認したところ、**紙のスキャンノイズ(微細な斑点)だけが残り、文字が完全に消えている**。前回の「薄くて読めない」より悪化している。追加した上下2%のレベル補正が、文字を含む階調を白側へ飛ばしていると考えられる。

## 修正方針

「まず素の描画で文字が出ることを確認し、そのうえで安全な補正だけを足す」順序で実装し直すこと。**いきなり補正込みで作らないこと。**

### 手順1: 素の描画で文字が見えることを確認する(必須の土台)

画素操作を**一切行わない**経路をまず作り、プレビューで文字が読めることを確認する。

- canvasを白で塗る → `page.render({canvasContext, viewport})` を実行 → **`await renderTask.promise` を必ずawaitする**(awaitが漏れていると描画途中のcanvasを取得して薄い/空の画像になる。ここも点検すること)
- **pdf.jsの `getPage(n)` は1始まり**である。0始まりで実装するとページがずれて別のページ(白紙等)を描画する。実際に描画したページ番号の配列を `console.log` に出力すること(例: `rendered pages: [1,2,3,4,5,6]`)
- そのまま `toDataURL("image/jpeg", 0.85)`
- この状態のプレビューで文字が読めない場合、原因は補正ではなくレンダリングにあるため、**補正の実装に進まずに報告すること**

### 手順2: 補正は「白飛ばし」のみに置き換える

上下2%の両側レベル補正(前回の指定)は**廃止**する。代わりに、文書スキャンで定番かつ安全な**白色点の正規化のみ**を行う。

```
// グレースケール化
v = 0.299R + 0.587G + 0.114B

// 紙の白色点を推定(上位側の代表値。暗部には一切触れない)
white = ヒストグラムの90パーセンタイル値
if (white < 160) white = 160        // 極端に暗い原稿での破綻防止

// 白を255へ持ち上げるだけ。黒側は0のまま動かさない
v' = clamp(v * 255 / white, 0, 255)
```

- **下限側(暗部)のクリッピングは絶対に行わない**。文字が消える直接の原因になる
- ループは `i += 4` で回し、**アルファチャンネル(data[i+3])は変更しない**
- 自動フォールバック判定: **補正前後で「輝度100未満の画素数」を数え、補正後が補正前の50%未満に減っていたら文字が消えたと判断し、補正を適用しない画像を採用する**。
  (注: 「平均輝度が高い」を判定に使ってはならない。紙が大半を占める疎なページでは正常な補正でも平均が240を超え、誤発動するため)

### 手順3: 管理者が補正を切り替えられるようにする

方式Dに Select を追加する。

- `なし(原本のまま)` / **`標準(白飛ばし)` ← 既定** / `強(白飛ばし+ガンマ1.4で文字を濃く)`
- 切り替えると**プレビューが即座に再生成**され、送信画像も切り替わる
- 「強」は `v'' = 255 * pow(v'/255, 1.4)` のみ。ここでも暗部のクリッピングはしない
  (注: ガンマは**1より大きい値が中間調を暗くする=文字を濃くする**方向。1未満は逆に薄くするので使わないこと。例: 輝度66の文字はγ1.4で約39に濃くなる)

これにより、補正が悪さをする原稿では管理者が「なし」を選んで回避できる。

### 手順4: プレビューの改善

- プレビューのサムネイル幅を **300px → 480px** に拡大し、下に「クリックで原寸表示」を明示
- サムネイルの下に、生成情報を小さく表示: `幅◯◯px × 高さ◯◯px / ◯◯KB / 補正: 標準`
- **1ページ目だけでなく、生成した全ページのサムネイルを横スクロールで並べる**(どのページが送られているかを管理者が把握できるように)

## 受け入れテスト(PR説明に記載)

1. `npm run build` 成功
2. 補正「なし」で実物PDFをアップロード → プレビューで文字が読める(ここが通らなければ他は無意味)
3. 補正「標準」でプレビューの背景が白くなり、**文字は消えずに残っている**
4. 補正「強」でも文字が消えない
5. 生成情報(幅・容量・補正種別)が表示され、幅が1900〜2100pxの範囲にある
6. 全ページのサムネイルが並び、想定したページ(基本1〜6、指定時は評価書から6)が生成されている
7. 補正「標準」で解析 → 事件番号・売却基準価額・買受可能価額・保証額・入札期間・開札日・所在地・固定資産税・都市計画税が抽出される
8. 送信ペイロードが3.5MB以下
9. `console.log` に描画したページ番号(1始まり)が出力され、想定ページと一致している
10. 文字が疎なページ(1ページ目)でも自動フォールバックが誤発動せず、補正「標準」が適用されている
