# 指示書: PR#9 の整理 — PDF解析(方式D)を撤去し、方式A・Cのみでマージ可能にする

対象: `agent/pr9-pdf-intake` ブランチ(PR #9)。**同じブランチ上で修正**すること。§0の絶対規則は引き続き適用。

## 方針

PDF解析(方式D)は、対象PDFがMRC構造(背景JPEG+CCITT G4ステンシルマスク)でpdf.jsが文字層を描画できず、解決にはPDF直送方式への設計変更とAPIトークン消費が必要と判明した。**当面は実装せず撤去**し、方式A(フォーム入力)と方式C(貼り付け一括取り込み)の2本立てで運用する。

将来の再開に備え、指示書 `CODEX_SPEC_pr9d_pdf_direct.md` はリポジトリに残す(削除しないこと)。

## 1. 撤去するもの

### クライアント
- 方式D「3点セットPDFから取り込み」のUI一式(ファイル選択、評価書の開始ページ入力、画質補正Select、サムネイル/iframeプレビュー、解析ボタン、関連state・ハンドラ)
- `pdfjs-dist` の依存・import(動的importを含む)・workerSrc設定
- canvasへのラスタライズ、グレースケール化、白点補正、ガンマ補正、文字消失検知などの画像処理コード一式
- `package.json` から `pdfjs-dist` を削除(`pdf-lib` も方式Dのみで使っていたなら削除)

### サーバー
- `api/auction-parse-pdf.js` を**ファイルごと削除**

### 方式B(CSV貼り付け)の統合
- 方式Bは方式Cに機能的に包含される(方式Cの解析はCSVも自動判別できる)。**方式Bのtextarea・ボタン・関連stateを削除**し、方式Cに一本化する
- ただし「**CSVテンプレートをダウンロード**」ボタンは方式Cの中に残す

### ドキュメント
- READMEから方式D・PDF解析・`pdfjs-dist`に関する記述を削除
- `.env.example` にPDF解析専用の変数を足していた場合は削除(`ANTHROPIC_API_KEY` は既存のAI市場調査で使うため**残す**)

## 2. 残すもの(削除しないこと)

- **方式A**: フォーム入力(1件修正用)。拡張フィールド(入札下限/買受可能価額、評価額、固定資産税、都市計画税、用途地域、建蔽率、容積率、占有状況、注記、売却基準価額の変更)は**すべて残す**。PDF解析が無くても管理者が手で入力でき、カード表示やシミュレーション連携に使われるため
- **方式C**: 貼り付け一括取り込み(表・CSV・タブ区切りの自動判別、日本語ヘッダーの吸収、日付/金額/面積の正規化、プレビュー表、行ごとのエラー判定と選択登録)。**これを既定の推奨方式として最上部に配置**する
- `supabase/schema_v5_auction_detail.sql`(拡張カラム。方式Aから入力されるため必要)
- 競売タブのカード表示(入札下限・評価額比・「再売却(減価あり)」バッジ)
- 「入札上限を計算」の連携拡張(price、固定資産税+都市計画税、築年からの残存償却年数)
- `api/auction-import.js`(管理者JWT + ADMIN_EMAILS の二重確認、id自動生成、upsert内訳返却)

## 3. UIの整理

管理者セクションの構成を次の順に整える。

1. 冒頭の注意文(BITの公表情報を確認のうえ転記/3点セットPDFの保存・転載は行わずリンクのみ登録)
2. **方式C: 表・CSVを貼り付けて一括登録(推奨)** — CSVテンプレートのダウンロードボタンを含む
3. **方式A: フォーム入力(1件ずつ・修正用)**
4. 登録済み一覧(直近20件・無効化ボタン)

「方式D」「方式B」という表記が画面・コード・コメントに残らないようにすること。番号の欠番が紛らわしいため、**UI上の見出しからは「方式A/C」の記号を外し**、「表・CSVを貼り付けて一括登録(推奨)」「フォームで1件ずつ入力」という説明的な見出しに変更してよい。

## 4. 受け入れテスト(PR説明に記載)

1. `npm run build` 成功。ビルド出力サイズが修正前より減っている
2. `pdfjs-dist` / `auction-parse-pdf` / 「方式D」への参照がコードベースに存在しない(grepで確認)
3. 管理者セクションに貼り付け欄とフォームの2つだけが表示される
4. 貼り付け一括取り込みが従来どおり動作する(タブ区切りの表・CSVの両方、日本語ヘッダー、金額/日付の正規化、エラー行の除外、id自動生成、重複時のupsert)
5. CSVテンプレートのダウンロードが機能する
6. フォーム入力で拡張フィールド(評価額・固定資産税・都市計画税・用途地域・建蔽率・容積率・占有状況・注記・買受可能価額)を入力して登録でき、カードに反映される
7. 「入札上限を計算」で価格と税(固定資産税+都市計画税)がシミュレーションに引き継がれる
8. 非管理者・未ログインには管理者セクションが表示されず、APIへの直接POSTは401/403
9. 一般ユーザー(Pro)の競売タブの検索・閲覧・フォロー・カレンダー連携に回帰がない
