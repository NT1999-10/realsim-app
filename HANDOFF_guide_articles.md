# 引継ぎ資料 — YOMU ガイド記事の執筆

> このファイルを新しいスレッドの冒頭で渡してください。これだけで、確認なしに執筆を始められるように書いてあります。
> 作成日 2026-09-10 / 前スレッドからの引き継ぎ

---

## 1. 依頼したいこと

不動産投資の収支シミュレーターSaaS「**YOMU**」のガイド記事を書きたい。目的は2つ。

1. **信頼の獲得** — 2,000万円級の判断に使う数字を出すアプリなので、「どう計算しているか」を開示しないと使ってもらえない
2. **獲得チャネル** — 「デッドクロス とは」「DSCR 計算方法」等の検索から、購入検討の直前にいる人を引き込む

最初に書くのは次の3本。**1本目から着手する。**

| # | 記事 | 役割 |
|---|---|---|
| 1 | YOMUの計算ロジックを全部公開します | 信頼の土台。アプリの `?` から恒久リンクする先 |
| 2 | 業者の提案書を30秒で検証する | 獲得の主力。LPのヒーローと同じ話を実例で |
| 3 | デッドクロスとは何か | 検索需要が最大。用語解説→自分の物件で計算 |

---

## 2. プロダクトの概要

**YOMU（旧称・現実派）** — 不動産投資の収支シミュレーター。個人開発。

名前は Yield（利回り）/ Occupancy（稼働）/ Mortgage（返済）/ Upkeep（維持）の頭字語で「読む」にかけている。

**中核**: 35年を**月次**（420ヶ月）で回す計算エンジン。金利上昇、家賃下落、退去と空室、原状回復、募集広告料、設備交換、大規模修繕、所得税・住民税、デッドクロス、売却出口の総当たりまでを織り込む。「業者提案の楽観シナリオ」と「保守シナリオ」を並べて差額を出すのが売り。

**主な機能**: 信号機診断（赤黄青＋日本語の理由文）／物件比較（IRR・CCR・DSCR）／感度分析・ストレステスト・出口最適化／運用管理（予実・設備台帳・繰上返済・申告CSV）／PDFレポート／AI市場調査13項目／国交省API相場照合／競売物件タブ

**料金**: Free ¥0 と Pro ¥1,480/月（税込）。Team は準備中。

---

## 3. 場所とアクセス方法

すべて **Public リポジトリ**なので、WebFetch で直接読める。ZIPを上げてもらう必要はない。

| | |
|---|---|
| アプリ本体 | `https://github.com/NT1999-10/realsim-app` → `https://realsim-app.vercel.app` |
| LP | `https://github.com/NT1999-10/realsim-lp` → `https://realsim-lp.vercel.app` |
| 生ファイルの読み方 | `https://raw.githubusercontent.com/NT1999-10/realsim-app/main/src/engine.js` |
| PRの差分 | `https://patch-diff.githubusercontent.com/raw/NT1999-10/realsim-app/pull/<番号>.diff` |

**注意**: `github.com/.../pull/N.diff` は302リダイレクトするので、上記の `patch-diff...` を直接叩く。GitHub API（`api.github.com`）は403で使えない。

**アプリのファイル構成（`src/`）**

```
App.jsx        3100行超の一枚岩。全画面がここ。分割リファクタは禁止
engine.js      計算エンジン（約180行）。★記事1本目の主題
theme.js       デザイントークン（T オブジェクト）
ui.jsx         共通UI（Field / Select / Kpi / cardSt / h2St / btnSt / TextField / LockCard）
logo.jsx       YomuMark / YomuWordmark / YomuLock
icons.jsx      アイコン（Icon / IconTile / IconSprite）
plan.js        PLANS とライセンス検証
demoData.js    サンプルデータ（PR#16で追加予定）
features/      auction / lead-intake / sashine（指値）/ souba（相場照合）
```

---

## 4. 記事について、前スレッドで決まったこと

**置き場所は自ドメイン。noteではない。**
`realsim-lp` に静的ページとして `/guide/<slug>.html` の形で追加する。理由は、SEOの評価がnote.comではなくYOMUに溜まること、そして**記事の途中に本物の30秒診断ウィジェットを埋め込めること**。「DSCRとは返済余裕率で、1.2を下回ると危険」と説明した直後に、その場で自分の物件のDSCRを計算させる。noteでは不可能。
noteには要約＋自ドメインへのリンクを転載する（拡散用、canonicalは自ドメイン）。

**アプリ内に記事誘導のポップアップは置かない。**
記事は獲得の道具で、登録済みユーザーには不要。代わりに、既存の `?` ポップオーバーに「詳しく読む →」を足して、その用語の記事だけに飛ばす。文脈のあるリンクだけ許可する。

**計算ロジックの記事にはバージョンを明記する。**
「2026年9月時点の計算仕様」と書く。前提を変えたら記事も直す必要がある。

**執筆の分担**
- 記事の中身・構成・文章 → Claude
- ページのテンプレート、CSS、Markdown→HTML化、ウィジェット埋め込みの配線、記事一覧・OGP・サイトマップ → Codex
- Claudeは **Markdown で書く**（HTMLの1/4以下のトークンで済む）

---

## 5. 執筆前にやること（未完了なら先にこれ）

前スレッドの最後で、次の2つをCodexに作らせる予定だった。**完了しているか確認すること。**

1. `docs/engine-spec.md` — Codexが `src/engine.js` を読んで、計算順序・各式・変数の意味を行番号つきで構造化した技術要約
2. `guide/` のページテンプレート — LPのデザイントークンを流用したCSS、ヘッダー・フッター・目次・記事内CTA・診断ウィジェット枠

**`docs/engine-spec.md` があれば、それを起点にする。ただし鵜呑みにしないこと。** もっともらしく間違えるのがこの手の作業の典型なので、記事に書く主要な式は `src/engine.js` の実コードで必ず裏を取る。無ければ `engine.js` を直接読む（180行なので負担は小さい）。

---

## 6. デザイン（記事ページを作るときの前提）

```
藍   #1E3E6B（主色） / #12233F（見出し） / #2F5488 / #4A74AB
山吹 #B8821F / #DBA53F（ラベル・アクセント）
朱   #C0392B（危険・警告・注目のみ。1画面3箇所以内）
緑   #12795A（良好）
面   #F7F7F4（地） / #FFFFFF（カード） / #FBFBF9
文字 #14202F / #41526A / #6B7B91
罫線 #E4E4DE / #D2D6DC
角丸 16 / 10 / 8 / 999px
```

**書体**: 見出し = Noto Serif JP 700 ／ 本文 = Zen Kaku Gothic New 500 ／ **数値 = Roboto Mono**（JetBrains Monoはゼロにドットが入り読みにくいので廃止済み）

**世界観**: 藍と山吹の方眼紙・帳簿テイスト。和のトーンを保ちつつ、データ密度でテック感を出す。**余白過多・情報量の薄さはNG**（依頼者が繰り返し指摘している点）。

**ロゴ**: 「障子越しの日輪」— 山吹の日輪を白い格子が分割し、手前を藍の屋根の稜線が横切る。ワードマークは Poppins Medium をアウトライン化したSVG（Oだけ山吹）。**横長の場所はワードマーク単体、正方形はマーク単体、両方並べるときはOを藍にする（金の丸は常にひとつ）**。実物は `realsim-app` の `src/logo.jsx` と `public/favicon.svg`。

---

## 7. 開発の進め方（記事ページの実装を出すとき）

依頼者は**ローカルにクローンを持っていない**。GitHubのWeb画面からファイルを直接アップロードしている。

**大きな変更**: 指示書を `CODEX_SPEC_*.md` としてリポジトリ**直下**に置き、Codexにファイル名で参照させる。命名は `CODEX_SPEC_pr<番号>_<内容>.md`。
**小さな修正**: チャットに直接貼れるテキストで渡す。

ファイルを渡すときは、**リネームと階層作りを済ませたZIP**にする。依頼者は展開して中身をドラッグするだけ、という状態にする。

### Codexに投げるときの必須事項

- **スクリーンショットを要求しない。Codexはブラウザを持たない。** 要求すると画面確認を試みて停止し、PR作成まで止まる。「スクリーンショットは不要です。ブラウザでの画面確認も不要です」と明記する
- 完了条件は **`npm run build` と `git diff` と `grep` で判定できるものだけ**にする。「〜が読みやすいこと」のような目視項目は「人間がプレビューで確認すること（Codexは判定しない）」という別の節に隔離する
- **範囲を必ず「〜だけ」で切る。** 切らないと全体を書き換えた巨大PRが返ってくる
- 前のPRをマージしてから次を投げる。並行させるとコンフリクトする

---

## 8. 踏んだ地雷（同じ轍を踏まないこと）

- **`src/plan.js` の `SALT = "genjitsuha-v1-salt-7f3a"`** — 旧ブランド名を含むが**絶対に変更しない**。発行済みライセンスキーが全部無効になる
- **`src/App.jsx` の `EXPENSE_CATS`** に `"建物管理費・修繕積立金"` があるが、これは**確定申告CSVの科目名**。UIラベルと一括置換すると申告集計が壊れる
- **タグで分断された文字列はgrepに引っかからない。** 旧ブランドが `現実<span>派</span>` と書かれていて、`現実派` の検索をすり抜けた。`現実` で検索すべきだった
- **LPと `plan.js` の食い違いが3回起きている。** LPが「AI市場調査 月2回」「感度分析はFree」と書いていたが、実装はどちらもPro限定だった。**プラン仕様を書くときは必ず `src/plan.js` を読んで裏を取る**
- **存在しないページにリンクしない。** LPのフッターに `/operator.html` を書いたが、法定ページは3つ（特商法・プライバシー・利用規約）しかなく404になった
- **Codexは指示書のコメントと実装コードの食い違いを正しく報告する。** 指示書を書くときは、散文の説明と提示コードを一致させる

---

## 9. 現在の状況（2026-09-10 時点）

**完了**: デザイン刷新シリーズ（PR-A〜PR-D）、YOMUリブランド、ロゴ実装、UI修正5点。LPも新デザイン＋新ロゴで公開済み。

**進行中/未確認**: PR-E（アイコンとイラストの適用・空状態の作り込み）の完了状況。`CODEX_SPEC_pr16_onboarding.md`（サンプルデータと詳細モードのFree開放）は指示書を作成済みで、実装はこれから。

**PR#16でプラン境界が変わる予定** — 記事に料金や機能の話を書くときは注意。

| | Free（PR#16後） | Pro |
|---|---|---|
| 詳細モード（30+パラメータ） | **開放予定** | ○ |
| 物件保存 | 3件 | 無制限 |
| AI市場調査 | サンプル閲覧のみ | 月10回 |
| 分析・運用管理・競売・PDFレポート | × | ○ |

**残タスク**: 法定3ページの中身確認、Stripe本番化（`PURCHASE_URL` がテストリンクのまま）、SALT変更、Supabase/Vercelの有料化、競売データ初期投入、URL移行（`realsim-app` / `realsim-lp` → YOMU名義）

---

## 10. 依頼者について

- 記事もLPも**日本語**。私（Claude）への指示も日本語
- **回りくどい前置きを嫌う。** 結論から書く
- **同意だけの返答を求めていない。** 反対意見や、より良い方法があるなら言ってほしいタイプ。実際、前スレッドでは提案の一部を却下して別案に差し替えている
- 「で、具体的に何をどこに置いて、何と言えばいい？」を繰り返し聞かれた。**手順は具体的に、ファイル名とコピペできる文面まで出す**
- 私（Claude）の側のミスも何度か指摘されている。推測で書かず、**実装を読んで裏を取ってから答える**こと

---

## 11. 最初の一手

```
YOMUのガイド記事を書きたいです。引継ぎ資料を読んで、
まず1本目「YOMUの計算ロジックを全部公開します」から始めてください。

docs/engine-spec.md があればそれを起点に、無ければ
src/engine.js を直接読んでください。いずれの場合も、
記事に書く主要な式は実コードで裏を取ってから書いてください。

出力はMarkdownで。HTML化はCodexに任せます。
```
